# SPDX-License-Identifier: MIT
---
- name: Allow restic user to run slapcat via sudo
  ansible.builtin.copy:
    content: |
      {{ restic_user }} ALL=(root) NOPASSWD: /usr/sbin/slapcat
    dest: /etc/sudoers.d/restic-openldap
    mode: '0440'
    validate: /usr/sbin/visudo -cf %s
