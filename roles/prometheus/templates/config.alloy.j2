{{ ansible_managed | comment('c') }}

prometheus.scrape "prometheus" {
    targets = [{"__address__" = "localhost:9090", "job" = "prometheus"}]
    scrape_interval = "15s"
    forward_to = [prometheus.remote_write.default.receiver]
}
